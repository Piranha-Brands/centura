
{%- if customer -%}

    {%- for collection_key in customer.metafields.pb_backend.collections.value -%}
      {% assign collection = collections[collection_key] %}
      {% comment %} <a href="{{ collections[collection_key].url  }}">  {{ collections[collection_key].title }} </a> {% endcomment %}
      <li><a href="{{ collection.url }}" class="{% if link_type == 'mobile'  %}menu-drawer{% else %}header{% endif %}__menu-item list-menu__item link link--text focus-inset caption-large">{{ collection.title }}</a></li>
    {%- endfor -%} 
    
{% else %}
    <li><a href="/collections/tops" class="{% if link_type == 'mobile'  %}menu-drawer{% else %}header{% endif %}__menu-item list-menu__item link link--text focus-inset caption-large">
        Tops
    </a></li>
    <li><a href="/collections/pants" class="{% if link_type == 'mobile'  %}menu-drawer{% else %}header{% endif %}__menu-item list-menu__item link link--text focus-inset caption-large">
        Pants
    </a></li>
    <li><a href="/collections/jackets" class="{% if link_type == 'mobile'  %}menu-drawer{% else %}header{% endif %}__menu-item list-menu__item link link--text focus-inset caption-large">
        Jackets
    </a></li>
    
{%- endif -%}